import AuthNav from "../../components/AuthNav/AuthNav";
import React from "react";

// I use this component when there will be user registration and we need to render a different header for registered users.

const Navbar = () => {
  return <AuthNav />;
};

export default Navbar;
